$('<%= "#followers_#{@user.id}" %>').replaceWith('<%= j render(partial: "users/followers", locals: { user: @user.reload }) %>')
$('<%= "#followers_#{current_user.id}" %>').replaceWith('<%= j render(partial: "users/followers", locals: { user: current_user.reload }) %>')
