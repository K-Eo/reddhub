div class="mx-auto py-4 col-12 col-md-7 col-lg-4"
  h3 class="text-center mb-4 font-weight-light"
    = title || "Sudo Form"

  div class="card"
    div class="card-body"
      = yield

  = render "devise/shared/links"
