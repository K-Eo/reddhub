- if user_signed_in? and current_user.likes?(pod)
  = link_to icon("fas", "heart"), pod_like_path(pod), class: "text-pink", remote: true, method: :delete
- else
  = link_to icon("far", "heart"), pod_like_path(pod), class: "text-secondary", remote: true, method: :post
