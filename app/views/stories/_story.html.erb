<div class="col-4 mb-4">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">
        <%= link_to story.title, story, class: 'text-dark' if policy(story).show? %>
      </h5>

      <p class="card-text">
        <%= story.content %>
      </p>
    </div>
  </div>
</div>
