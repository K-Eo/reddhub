var target = document.querySelector("div#reaction_<%= @reactable.id %>")
target.insertAdjacentHTML('beforebegin', '<%= j render(partial: "reactions/action", locals: { reaction: @reaction, reactable: @reactable }) %>')
target.remove()
