div class="messages"
  - flash.each do |key, value|
    div class="alert alert-#{key} fade show border-0 rounded-0 js-actionable" role="alert" data-controller="alert" data-action="click->alert#close"
      div class="container" = value
